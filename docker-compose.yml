services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: freely
      POSTGRES_PASSWORD: freely
      POSTGRES_DB: freely
    ports:
      - "5432:5432"
    volumes:
      - freely_pgdata:/var/lib/postgresql/data

volumes:
  freely_pgdata:
